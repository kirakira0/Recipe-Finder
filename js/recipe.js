const main = document.querySelector("#main"); 

window.addEventListener('load', e =>{
 
    //message.style.transform = 'rotate(200deg)'; 
    //message.style.transform = 'rotate(270deg)'; 
    main.style.opacity = 1; 
})